package com.NagiGroup.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JacksonConfig {
	/*
	 * @Bean public ObjectMapper objectMapper() { ObjectMapper objectMapper = new
	 * ObjectMapper(); objectMapper.registerModule(new JavaTimeModule()); //
	 * Register the JavaTimeModule return objectMapper; }
	 */
}
